sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t yanivdror/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true yanivdror/docker-react npm run test
